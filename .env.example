# Gemini backup (required until own model weights are ready)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Own model (leave empty until weights are trained and placed here)
MODEL_PATH=
CONFIDENCE_THRESHOLD=0.7

# Server
PORT=8000
LOG_LEVEL=info
